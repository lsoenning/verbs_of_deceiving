---
title: "R Notebook"
output: html_notebook
---


# Models for mid-term diachronic perspective

```{r}
all_data <- read.csv("./data/all_data.csv", stringsAsFactors = T)
mid_data <- subset(all_data, year >= 1820)

# recode drama -> spoken
mid_data$genre[mid_data$genre == "Drama"] <- "Spoken"
mid_data$genre_short[mid_data$genre_short == "dra"] <- "spo"
mid_data <- droplevels(mid_data)

str(mid_data)
```

## Loss of interpretators

### Deceive

```{r}
m_mid_deceive <- glm(
  interpretator_present ~ year_c + variety + transitivity + negation + genre_short, 
  data=mid_data, subset=verb == "deceive", family="binomial")

arm::display(m_mid_deceive)
```

```{r fig.width=13, fig.height=3}
plot(effects::predictorEffect(
  "year_c",
  m_mid_deceive, 
  confidence.level=.95), 
  axes=list(y=list(type="response", lim=c(0,1))),
  lattice=list(array=list(row=1, col=1, nrow=1, ncol=5, more=T)),
  main="Year", xlab="")

plot(effects::predictorEffect(
  "variety",
  m_mid_deceive, 
  confidence.level=.95), 
  axes=list(y=list(type="response",lim=c(0,1))),
  lattice=list(array=list(row=1, col=2, nrow=1, ncol=5, more=T)),
  main="Variety", xlab="", ylab="")

plot(effects::predictorEffect(
  "negation",
  m_mid_deceive, 
  confidence.level=.95), 
  axes=list(y=list(type="response",lim=c(0,1))),
  lattice=list(array=list(row=1, col=3, nrow=1, ncol=5, more=T)),
  main="Negation", xlab="", ylab="")

plot(effects::predictorEffect(
  "transitivity",
  m_mid_deceive, 
  confidence.level=.95), 
  axes=list(y=list(type="response",lim=c(0,1))),
  lattice=list(array=list(row=1, col=4, nrow=1, ncol=5, more=T)),
  main="Transitivity", xlab="", ylab="")

plot(effects::predictorEffect(
  "genre_short",
  m_mid_deceive, 
  confidence.level=.95), 
  axes=list(y=list(type="response",lim=c(0,1))),
  lattice=list(array=list(row=1, col=5, nrow=1, ncol=5, more=F)),
  main="Genre", xlab="", ylab="")
```

### Delude


```{r}
m_mid_delude <- glm(
  interpretator_present ~ year_c + variety + transitivity + negation + genre_short, 
  data=mid_data, subset=verb == "delude", family="binomial")

arm::display(m_mid_delude)
```

```{r fig.width=13, fig.height=3}
plot(effects::predictorEffect(
  "year_c",
  m_mid_delude, 
  confidence.level=.95), 
  axes=list(y=list(type="response", lim=c(0,1))),
  lattice=list(array=list(row=1, col=1, nrow=1, ncol=5, more=T)),
  main="Year", xlab="")

plot(effects::predictorEffect(
  "variety",
  m_mid_delude, 
  confidence.level=.95), 
  axes=list(y=list(type="response",lim=c(0,1))),
  lattice=list(array=list(row=1, col=2, nrow=1, ncol=5, more=T)),
  main="Variety", xlab="", ylab="")

plot(effects::predictorEffect(
  "negation",
  m_mid_delude, 
  confidence.level=.95), 
  axes=list(y=list(type="response",lim=c(0,1))),
  lattice=list(array=list(row=1, col=3, nrow=1, ncol=5, more=T)),
  main="Negation", xlab="", ylab="")

plot(effects::predictorEffect(
  "transitivity",
  m_mid_delude, 
  confidence.level=.95), 
  axes=list(y=list(type="response",lim=c(0,1))),
  lattice=list(array=list(row=1, col=4, nrow=1, ncol=5, more=T)),
  main="Transitivity", xlab="", ylab="")

plot(effects::predictorEffect(
  "genre_short",
  m_mid_delude, 
  confidence.level=.95), 
  axes=list(y=list(type="response",lim=c(0,1))),
  lattice=list(array=list(row=1, col=5, nrow=1, ncol=5, more=F)),
  main="Genre", xlab="", ylab="")
```


# Fool

```{r}
m_mid_fool <- glm(
  interpretator_present ~ year_c + variety + transitivity + negation + genre_short, 
  data=mid_data, subset=verb == "fool", family="binomial")

arm::display(m_mid_fool)
```

```{r fig.width=13, fig.height=3}
plot(effects::predictorEffect(
  "year_c",
  m_mid_fool, 
  confidence.level=.95), 
  axes=list(y=list(type="response", lim=c(0,1))),
  lattice=list(array=list(row=1, col=1, nrow=1, ncol=5, more=T)),
  main="Year", xlab="")

plot(effects::predictorEffect(
  "variety",
  m_mid_fool, 
  confidence.level=.95), 
  axes=list(y=list(type="response",lim=c(0,1))),
  lattice=list(array=list(row=1, col=2, nrow=1, ncol=5, more=T)),
  main="Variety", xlab="", ylab="")

plot(effects::predictorEffect(
  "negation",
  m_mid_fool, 
  confidence.level=.95), 
  axes=list(y=list(type="response",lim=c(0,1))),
  lattice=list(array=list(row=1, col=3, nrow=1, ncol=5, more=T)),
  main="Negation", xlab="", ylab="")

plot(effects::predictorEffect(
  "transitivity",
  m_mid_fool, 
  confidence.level=.95), 
  axes=list(y=list(type="response",lim=c(0,1))),
  lattice=list(array=list(row=1, col=4, nrow=1, ncol=5, more=T)),
  main="Transitivity", xlab="", ylab="")

plot(effects::predictorEffect(
  "genre_short",
  m_mid_fool, 
  confidence.level=.95), 
  axes=list(y=list(type="response",lim=c(0,1))),
  lattice=list(array=list(row=1, col=5, nrow=1, ncol=5, more=F)),
  main="Genre", xlab="", ylab="")
```